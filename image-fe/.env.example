# Copy to .env and adjust if ComfyUI/Ollama aren't on the default host/port.
COMFYUI_URL=http://host.docker.internal:8188
LLM_URL=http://host.docker.internal:11434
# Must match the name you `ollama pull`ed (`ollama list` to check).
LLM_MODEL=nemomix-unleashed-12b
